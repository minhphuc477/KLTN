# DiT + continuous Flow Matching ablation.
config_version: 1

training:
  stage: diffusion

diffusion:
  denoiser_backbone: dit
  training_objective: flow_matching
  prediction_type: epsilon
  min_snr_gamma: 0.0
  dit_depth: 4
  dit_patch_size: 1
  dit_mlp_ratio: 4.0
