# Neural-stage-only ablation.
# Keeps diffusion + LogicNet active and disables runtime symbolic repair.
config_version: 1

training:
  stage: diffusion

generation:
  apply_repair: false
  use_neural_guided_repair: false
